
(variable white_space)

(defun bar ()
     (fill_char_array (store (make_array 1 128) white_space) "   
"))

(defun foo ()
     (delete tg 10000)
     (print_msg (insert_ioa "char: ^2w" (nthr current_buffer 0) tg) 0)
     (set_loc current_buffer (find_first_in_fa current_buffer white_space)))

^L